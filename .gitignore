# Godot 4+ specific ignores
.godot/
# *.import files are import settings + uids for the shipped PNG/WAV/MP3 assets; commit them
# once an editor pass has generated them so every clone imports identically.
/android/

# Summer Engine local-only state
.summer/local/

# Editor-generated backups
*.bak

# Local MCP server config (machine-specific paths; not for the shared template)
.mcp.json
